{
  "name": "cleanup-unverified-users",
  "version": "1.0.0",
  "description": "Deletes unverified Appwrite Auth users older than 24 hours",
  "main": "src/main.js",
  "type": "module",
  "scripts": {
    "format": "prettier --write ."
  },
  "dependencies": {
    "node-appwrite": "^14.1.0"
  },
  "devDependencies": {
    "prettier": "^3.2.5"
  }
}
